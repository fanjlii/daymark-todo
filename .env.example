# Copied into apps/web/.env.local for local development.
NEXT_PUBLIC_API_URL=http://localhost:8787

# Optional Cloudflare Worker variable. Vercel preview URLs and the local
# frontend are accepted automatically.
CORS_ORIGINS=https://your-production-domain.example
